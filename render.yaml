services:
  - type: web
    name: mobile-quant-h5
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py
    envVars:
      - key: MOMO_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
